

    <p>Click the button to get your coordinates.</p>

    <button onclick="getLocation()">Try It</button>

    <p id="demo"></p>

    <script>function getLocation() {
  if (navigator.geolocation) {


    navigator.geolocation.getCurrentPosition(showPosition);
  } else {

    console.log("please allow the permission for the navigation")

  //TODO: build the logic if the location permission is not available
    x.innerHTML = "Geolocation is not supported by this browser.";
  }
}

function showPosition(position) {
  showLocationOnMap({latitude: position.coords.latitude ,longitude:position.coords.longitude})

}


function showLocationOnMap(coordinates){
	const API_KEY = "AIzaSyDNDxDy0aCcQGKWys5m5XJfBGPXoEb6dbY"
	const src =  `https://www.google.com/maps/embed/v1/place?key=${API_KEY}&q=${coordinates.latitude},${coordinates.longitude}`
    const mapIframe = document.getElementById("gmap_canvas");
    	  mapIframe.src= src
}</script>

    <iframe width="800" height="500" id="gmap_canvas" src="" frameborder="0" scrolling="no" marginheight="0" marginwidth="0"></iframe>

